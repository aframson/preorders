# Dependencies allowed to run install scripts (pnpm 10+ blocks them by default).
# Without these, Vercel fails with ERR_PNPM_IGNORED_BUILDS.
allowBuilds:
  '@depot/cli': true
  esbuild: true
  sharp: true
  unrs-resolver: true
