# Dependencies allowed to run install scripts. Both belong to the Trigger.dev
# CLI: esbuild fetches its platform binary and @depot/cli its build client.
allowBuilds:
  '@depot/cli': true
  esbuild: true
